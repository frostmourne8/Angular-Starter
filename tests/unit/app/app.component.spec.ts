import { AppComponentController } from 'app/app.component';

describe('AppComponent', () => {

   let controller: AppComponentController;

   beforeEach(() => {
       controller = new AppComponentController();
   }); 
});